test-upload-size-limit:
  description: "Rucio upload size limit"
  module_name: "tasks.tests.upload_size_limit"
  class_name: "UploadSizeLimit"
  enabled: true
  args:
  kwargs:
    rse: "STFC_STORM"
    scope: "aussrc.testing"
    lifetime: 600
    min_size: 100000
    max_size: 1000000000
    factor: 10
